<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - TaskMaster</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
     body {
      background: url('https://i.ibb.co/WpzmJYHX/pexels-brunogobofoto-3854816.jpg') no-repeat center center fixed;
      background-size: cover;
      opacity: 0.85;
    }

    @keyframes gradientAnimation {
      0%, 100% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
    }

    .neon-border {
      border: 2px solid transparent;
      background: linear-gradient(130deg, #f9f9f9, #f0ff7d);
      background-origin: border-box;
      background-clip: padding-box, border-box;
      box-shadow: 0 4px 15px rgba(255, 110, 196, 0.4);
    }
  </style>
</head>
<body class="flex items-center justify-center min-h-screen text-gray-600">

  <!-- Login Form -->
  <div class="bg-white bg-opacity-80 backdrop-blur-md rounded-2xl p-8 w-full max-w-md neon-border shadow-xl">
    <h1 class="text-4xl font-extrabold text-center mb-6 text-transparent bg-clip-text bg-gradient-to-r from-pink-500 to-purple-500 animate-pulse">
      TaskMaster
    </h1>
    <p class="text-center text-gray-500 mb-6">Welcome back, Master your tasks!</p>
    
    <% if (error) { %>
      <div class="bg-red-500 text-gray-600 p-4 rounded mb-4">
        <p><%= error %></p>
      </div>
    <% } %>

    <form action="/login" method="POST" class="space-y-6">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
      
      <div>
        <label for="email" class="block text-sm font-medium text-gray-600 mb-2">Email</label>
        <input type="email" name="email" id="email" required 
               class="w-full p-3 rounded-md bg-transparent border border-gray-500 text-gray-600 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-500">
      </div>

      <div>
        <label for="password" class="block text-sm font-medium text-gray-600 mb-2">Password</label>
        <input type="password" name="password" id="password" required
               class="w-full p-3 rounded-md bg-transparent border border-gray-500 text-gray-600 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-purple-500">
      </div>

      <button type="submit" class="w-full bg-gradient-to-r from-pink-500 to-purple-500 text-gray-200 py-3 rounded-md hover:opacity-90 transition-opacity duration-300 shadow-lg">
        Login
      </button>
    </form>

    <p class="mt-4 text-center text-sm text-gray-600">
      <a href="/register" class="text-pink-400 hover:text-pink-300 transition-colors duration-200">Don't have an account? Register here.</a>
    </p>
  </div>

</body>
</html>